# Deprecato: il build web usa direttamente ../src dalla root del repo.
# Mantenuto solo per compatibilità se qualche script lo invoca ancora.
console.log("[prepare] OK — sorgente condivisa: src/ nella root del repo (nessuna copia).");
